describe("Data Type", function () {
  it("should must declarate", function () {
    let name: string = "Muhammad Fadly";
    let balance: number = 1000;
    let isVip: boolean = true;

    console.info(name);
    console.info(balance);
    console.info(isVip);
  });
});
